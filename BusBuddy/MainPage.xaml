<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BusBuddy.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="CreateDbBtn"
                Text="Create Database"
                Clicked="OnCreateDbClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="TestBtn"
                Text="Test"
                Clicked="OnTestButtonClicked"
                HorizontalOptions="Fill" />

            <!-- Navigation Buttons for CRUD -->
            <Label Text="Routes" Style="{StaticResource SubHeadline}" />
            <Button Text="Create Route" Clicked="OnCreateRouteClicked" />
            <Button Text="Read Route" Clicked="OnReadRouteClicked" />
            <Button Text="Update Route" Clicked="OnUpdateRouteClicked" />
            <Button Text="Delete Route" Clicked="OnDeleteRouteClicked" />

            <Label Text="Buses" Style="{StaticResource SubHeadline}" />
            <Button Text="Create Bus" Clicked="OnCreateBusClicked" />
            <Button Text="Read Bus" Clicked="OnReadBusClicked" />
            <Button Text="Update Bus" Clicked="OnUpdateBusClicked" />
            <Button Text="Delete Bus" Clicked="OnDeleteBusClicked" />

            <Label Text="Drivers" Style="{StaticResource SubHeadline}" />
            <Button Text="Create Driver" Clicked="OnCreateDriverClicked" />
            <Button Text="Read Driver" Clicked="OnReadDriverClicked" />
            <Button Text="Update Driver" Clicked="OnUpdateDriverClicked" />
            <Button Text="Delete Driver" Clicked="OnDeleteDriverClicked" />

            <ListView x:Name="DataListView" HeightRequest="200"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
